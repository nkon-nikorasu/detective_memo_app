<% flash.each do |type, message| %>
  <div class="
    p-4 rounded mb-4
    <%= type == 'notice' ? 'bg-blue-100 text-blue-800 mx-20 border-5' : '' %>
    <%= type == 'alert' ? 'bg-red-100 text-red-800 mx-20 border-5' : '' %>
  ">
    <%= message %>
  </div>
<% end %>