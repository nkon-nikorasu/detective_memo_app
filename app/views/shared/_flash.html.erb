<% flash.each do |type, message| %>
  <div class="
    p-4 rounded mb-4
    <%= type == 'notice' ? 'bg-blue-100 text-blue-800' : '' %>
    <%= type == 'alert' ? 'bg-red-100 text-red-800' : '' %>
  ">
    <%= message %>
  </div>
<% end %>